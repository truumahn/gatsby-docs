const size = {
  xs: '599px',
  sm: '600px',
  md: '900px',
  lg: '1200px',
  xl: '1800px',
};

export const xsMax = `(max-width: ${size.sm})`;
export const sm = `(min-width: ${size.sm})`;
export const smMax = `(max-width: ${size.md})`;
export const md = `(min-width: ${size.md})`;
export const mdMax = `(max-width: ${size.lg})`;
export const lg = `(min-width: ${size.lg})`;
export const lgMax = `(max-width: ${size.xl})`;
export const xl = `(min-width: ${size.xl})`;
